# Как открыть DevTools в Electron

## Способ 1: Автоматически (уже настроено) ✅

DevTools теперь открываются автоматически при запуске приложения.

Просто запустите:
```bash
npm start
```

DevTools откроются автоматически в отдельном окне или внизу основного окна.

---

## Способ 2: Горячая клавиша

**Нажмите `F12`** в окне Electron приложения.

Или **`Ctrl+Shift+I`** (Windows/Linux) или **`Cmd+Option+I`** (Mac).

---

## Способ 3: Через меню (если доступно)

1. Кликните правой кнопкой мыши в окне приложения
2. Выберите "Inspect Element" или "Inspect"
3. Или через меню: View → Toggle Developer Tools

---

## Способ 4: Режим разработки

Запустите приложение в режиме разработки:
```bash
npm run dev
```

DevTools откроются автоматически.

---

## Что делать в DevTools

### 1. Откройте вкладку Console

В DevTools найдите вкладку **Console** (Консоль).

Здесь вы увидите все логи из JavaScript кода UI:
- `📤 Отправка сообщения через openrouter`
- `📥 Результат получен: ...`
- Ошибки, если они есть

### 2. Проверьте вкладку Network (опционально)

Если нужно проверить сетевые запросы к OpenRouter, откройте вкладку **Network**.

---

## Как закрыть DevTools

- Нажмите `F12` снова
- Или кликните на крестик в окне DevTools
- Или закомментируйте строку в `main.js`:
  ```javascript
  // mainWindow.webContents.openDevTools();
  ```

---

## Если DevTools не открываются

1. **Проверьте, что приложение запущено:**
   ```bash
   npm start
   ```

2. **Попробуйте горячую клавишу `F12`**

3. **Проверьте `main.js`:**
   Убедитесь, что есть строка:
   ```javascript
   mainWindow.webContents.openDevTools();
   ```

4. **Перезапустите приложение**

---

**После открытия DevTools вы увидите консоль браузера с логами!**







