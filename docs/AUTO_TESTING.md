# Автоматическое тестирование функций

## Запуск тестов

```bash
npm test
```

или

```bash
npm run test:chat
```

## Что тестируется

### 1. Парсер файлов (`FileParser`)
- Парсинг файла с путем в заголовке блока кода
- Парсинг файла с путем в комментарии
- Парсинг HTML блоков
- Нормализация путей (Windows/Linux)

### 2. Операции с файлами
- Создание файла
- Чтение файла
- Изменение файла
- Удаление файла

### 3. Контекст чата (`ChatContextManager`)
- Извлечение упоминания файла из сообщения
- Извлечение запросов на чтение файлов
- Получение контекста проекта
- История чата

### 4. Паттерны удаления файлов
- "удали main.js"
- "удалить файл test.js"
- "delete file.txt"
- "remove src/main.js"
- "удали файл: test.js"

### 5. Паттерны чтения файлов
- "покажи содержимое main.js"
- "что в файле ui.js"
- "прочитай src/main.js"
- "что находится в test.js"
- "покажи код файла main.js"

### 6. Нормализация путей
- Обработка обратных слешей (Windows)
- Обработка прямых слешей (Linux/Mac)
- Удаление префиксов `src/`, `lib/`, `./`
- Корректная обработка относительных путей

## Результаты тестов

После запуска тестов вы увидите:
- ✅ Пройдено: количество успешных тестов
- ❌ Провалено: количество проваленных тестов
- 📊 Всего: общее количество тестов
- Список ошибок (если есть)

## Добавление новых тестов

Чтобы добавить новый тест, откройте `tests/auto-test-chat-functions.js` и добавьте функцию:

```javascript
async function testNewFeature() {
  console.log('\n=== Тест новой функции ===\n');
  
  try {
    // Ваш тест
    const result = someFunction();
    logTest('Название теста', result === expected);
  } catch (error) {
    logTest('Название теста', false, error);
  }
}
```

Затем добавьте вызов в `runAllTests()`:

```javascript
await testNewFeature();
```

## Примечания

- Тесты не требуют запуска Electron
- Тесты работают с файловой системой (создают временные файлы)
- Временные файлы автоматически удаляются после тестов
- Тесты можно запускать в CI/CD пайплайне




