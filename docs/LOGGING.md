# Система логирования VibeCode

## 📋 Обзор

Централизованная система логирования с сохранением в файлы и отслеживанием ошибок.

## 🎯 Возможности

- ✅ **Уровни логирования**: ERROR, WARN, INFO, DEBUG
- ✅ **Сохранение в файлы** с автоматической ротацией
- ✅ **Полная информация об ошибках**: файл, строка, стек вызовов
- ✅ **Структурированные логи** в формате JSON
- ✅ **Автоматическая очистка** старых логов (старше 7 дней)
- ✅ **Правильная кодировка** для Windows (UTF-8)

## 📁 Расположение логов

Логи сохраняются в директории: `logs/vibecode-YYYY-MM-DD.log`

Пример: `logs/vibecode-2025-12-06.log`

## 🔧 Использование

### Базовое использование

```javascript
const { getLogger } = require('./lib/logger');
const logger = getLogger();

// Логирование информации
logger.info('Приложение запущено');

// Логирование предупреждения
logger.warn('База знаний недоступна');

// Логирование ошибки
try {
  // какой-то код
} catch (error) {
  logger.error('Ошибка выполнения операции', error);
}

// Логирование отладки
logger.debug('Текущее состояние', { data: someData });
```

### Логирование с данными

```javascript
logger.info('Пользователь выполнил действие', {
  userId: 123,
  action: 'generate-project',
  timestamp: Date.now()
});
```

### Логирование ошибок с полным контекстом

```javascript
try {
  await someAsyncOperation();
} catch (error) {
  logger.error('Ошибка выполнения операции', error, {
    operation: 'generate-project',
    task: taskDescription,
    config: configPath
  });
}
```

## 📊 Уровни логирования

### ERROR
Критические ошибки, которые требуют внимания.

```javascript
logger.error('Не удалось подключиться к базе данных', error);
```

### WARN
Предупреждения о потенциальных проблемах.

```javascript
logger.warn('База знаний недоступна, работаем без неё');
```

### INFO
Информационные сообщения о работе приложения.

```javascript
logger.info('Проект успешно сгенерирован', { filesCount: 10 });
```

### DEBUG
Отладочная информация для разработки.

```javascript
logger.debug('Текущее состояние агента', { state: agentState });
```

## 🔍 Просмотр логов

### В консоли
Логи автоматически выводятся в консоль с цветовой подсветкой:
- 🔴 ERROR - красный
- 🟡 WARN - желтый
- 🔵 INFO - голубой
- ⚪ DEBUG - серый

### В файлах
Логи сохраняются в JSON формате для удобного парсинга:

```json
{
  "timestamp": "2025-12-06T01:21:28.637Z",
  "level": "ERROR",
  "message": "Ошибка генерации проекта",
  "file": "src/main.js",
  "line": 95,
  "error": {
    "message": "Cannot read property 'generateProject' of null",
    "stack": "Error: Cannot read property...",
    "name": "TypeError"
  },
  "data": {
    "task": "generate project"
  }
}
```

## 🛠️ Настройка

### Инициализация с параметрами

```javascript
const { initLogger } = require('./lib/logger');
const logger = initLogger({
  minLevel: 'info',        // Минимальный уровень (debug, info, warn, error)
  consoleOutput: true,      // Вывод в консоль
  fileOutput: true,         // Сохранение в файл
  maxFileSize: 10 * 1024 * 1024,  // Максимальный размер файла (10MB)
  maxFiles: 5              // Количество файлов для ротации
});
```

### Изменение уровня логирования

```javascript
// Показывать только ошибки и предупреждения
const logger = initLogger({ minLevel: 'warn' });

// Показывать все логи включая отладку
const logger = initLogger({ minLevel: 'debug' });
```

## 📝 Формат логов

Каждая запись лога содержит:

- `timestamp` - ISO дата и время
- `level` - уровень логирования (ERROR, WARN, INFO, DEBUG)
- `message` - текстовое сообщение
- `file` - файл, где произошло событие
- `line` - номер строки
- `data` - дополнительные данные (опционально)
- `error` - информация об ошибке (если есть)

## 🔄 Ротация логов

Логи автоматически ротируются при достижении максимального размера файла:

- `vibecode-2025-12-06.log` - текущий файл
- `vibecode-2025-12-06.log.1` - предыдущий
- `vibecode-2025-12-06.log.2` - еще более старый
- и т.д.

## 🧹 Очистка старых логов

Автоматическая очистка логов старше 7 дней при старте приложения:

```javascript
logger.cleanOldLogs(7); // Удалить логи старше 7 дней
```

## 📖 Примеры использования в коде

### В main.js

```javascript
const { initLogger } = require('../lib/logger');
const logger = initLogger();

logger.info('Инициализация приложения VibeCode');

try {
  await initAgents();
  logger.info('Агенты инициализированы');
} catch (error) {
  logger.error('Ошибка инициализации агентов', error);
}
```

### В agents

```javascript
const { getLogger } = require('../lib/logger');
const logger = getLogger();

logger.info('Начало генерации проекта', { task });
try {
  const result = await generateProject();
  logger.info('Проект сгенерирован', { filesCount: result.files.length });
} catch (error) {
  logger.error('Ошибка генерации проекта', error, { task });
}
```

### В router

```javascript
const { getLogger } = require('../lib/logger');
const logger = getLogger();

try {
  const response = await queryOpenRouter(model, prompt);
  logger.debug('Ответ получен', { length: response.length });
} catch (error) {
  logger.error('Ошибка запроса к OpenRouter', error, { model, promptLength: prompt.length });
}
```

## 🐛 Отладка ошибок

При возникновении ошибки логгер автоматически сохраняет:

1. **Сообщение об ошибке**
2. **Полный стек вызовов**
3. **Файл и строку** где произошла ошибка
4. **Дополнительный контекст** (если передан)

Это позволяет быстро найти и исправить проблему.

## 📚 Дополнительные методы

### Получение последних логов

```javascript
const recentLogs = logger.getRecentLogs(100); // Последние 100 записей
```

### Очистка старых логов

```javascript
logger.cleanOldLogs(7); // Удалить логи старше 7 дней
```

## ⚙️ Рекомендации

1. **Используйте правильный уровень**:
   - ERROR - только для критических ошибок
   - WARN - для предупреждений
   - INFO - для важных событий
   - DEBUG - для отладки

2. **Добавляйте контекст**:
   ```javascript
   logger.error('Ошибка сохранения файла', error, {
     filePath: filePath,
     fileSize: fileSize
   });
   ```

3. **Логируйте ошибки с полным стеком**:
   ```javascript
   logger.error('Ошибка выполнения операции', error);
   ```

4. **Не логируйте чувствительные данные**:
   ```javascript
   // ❌ Плохо
   logger.info('Пользователь авторизован', { password: userPassword });
   
   // ✅ Хорошо
   logger.info('Пользователь авторизован', { userId: user.id });
   ```

## 🔗 Связанные файлы

- `lib/logger.js` - основной модуль логгера
- `logs/` - директория с логами
- `src/main.js` - пример использования в главном процессе

---

**Система логирования готова к использованию! 🎉**



