# Исправление проблемы с отображением файлов в src/

## Проблема

AI не видел существующие файлы в папке `src/`. При запросе "какие файлы находятся в папке /src?" AI отвечал, что там только директории, но нет файлов.

## Причина

Функция `analyzeProjectStructure()` в `lib/chat-context.js` сканировала файлы, но:
1. Не показывала файлы в корне `src/` отдельно
2. Не группировала файлы по поддиректориям
3. Не учитывала различия в путях на Windows (обратные слеши vs прямые)

## Решение

### 1. Улучшена функция `analyzeProjectStructure()`

Добавлена детальная структура директории `src/`:
- **Файлы в корне src/**: показываются все файлы напрямую в `src/`
- **Поддиректории с файлами**: каждая поддиректория показывается отдельно со списком файлов
- **Нормализация путей**: автоматическая обработка путей для Windows

### 2. Формат отображения

Теперь структура показывается так:

```
📁 Структура директории src/:

Файлы в корне src/:
  📄 filepath
  📄 index.html
  📄 main.js
  📄 preload.js
  📄 style.css
  📄 testi.txt
  📄 ui.js

📂 src/agents/:
  📄 explain.js
  📄 refactor.js

📂 src/ai/:
  📄 router.js

📂 src/ui/:
  📄 index.html
  📄 ui.js
```

## Результат

Теперь AI видит:
- ✅ Все файлы в корне `src/`
- ✅ Все файлы в поддиректориях `src/`
- ✅ Структуру директорий с группировкой файлов
- ✅ Правильные пути на Windows

## Тестирование

После исправления AI правильно отвечает на вопросы:
- "какие файлы находятся в папке /src?"
- "покажи структуру src/"
- "какие файлы в src/agents/?"




