# Инструкция по самопрограммированию Cursor X

## ✅ Готовность системы

Система полностью готова к самопрограммированию:
- ✅ Vision.md загружен (9,256 символов)
- ✅ Roadmap.md загружен (8,396 символов)
- ✅ SelfDev Agent готов
- ✅ AI Router настроен
- ✅ UI интегрирован

---

## 🚀 Как запустить самопрограммирование

### Шаг 1: Подготовка AI провайдера

**Вариант A: LM Studio (локальный)**
1. Запустите LM Studio
2. Загрузите модели:
   - `deepseek/deepseek-r1-0528-qwen3-8b`
   - `nomic-ai-gpt4all-falcon`
3. Убедитесь, что сервер слушает на порту 1234

**Вариант B: OpenRouter (API)**
1. Убедитесь, что есть интернет-соединение
2. API ключ уже настроен в `config.json`
3. Выберите OpenRouter в UI

### Шаг 2: Запуск приложения

```bash
npm start
```

Или в режиме разработки (с DevTools):
```bash
npm run dev
```

### Шаг 3: Выбор провайдера

В боковой панели (слева) выберите AI Провайдер:
- **LM Studio (локальный)** - для локальных моделей
- **OpenRouter (API)** - для внешних моделей

### Шаг 4: Запуск Self-Build

1. **Нажмите кнопку "Self-Build"** в боковой панели
2. Система автоматически:
   - Прочитает Vision.md и Roadmap.md
   - Сформирует промпт для AI
   - Выберет оптимальную модель
   - Сгенерирует файлы проекта
   - Сохранит их в `/src`

---

## 📋 Что происходит при Self-Build

### Процесс самопрограммирования:

1. **Чтение документов:**
   ```
   Vision.md → понимание целей проекта
   Roadmap.md → понимание плана развития
   ```

2. **Анализ проекта:**
   ```
   Анализ текущей структуры → понимание что уже есть
   ```

3. **Формирование промпта:**
   ```
   Vision + Roadmap + Структура проекта → Промпт для AI
   ```

4. **Генерация кода:**
   ```
   AI Router → Выбор модели → Генерация файлов
   ```

5. **Парсинг и сохранение:**
   ```
   Парсинг ответа AI → Извлечение файлов → Сохранение в /src
   ```

6. **Логирование:**
   ```
   Все действия логируются в /logs
   ```

---

## 🎯 Примеры использования

### Пример 1: Генерация нового компонента

1. Запустите приложение
2. Выберите провайдер
3. Нажмите "Self-Build"
4. Система сгенерирует файлы на основе Vision и Roadmap

### Пример 2: Через чат-интерфейс

Вы можете также использовать чат для конкретных задач:

```
"Создай новый агент для рефакторинга кода"
```

Система:
- Определит тип задачи
- Выберет модель
- Сгенерирует код
- Сохранит файл

---

## ⚙️ Настройка самопрограммирования

### Изменение Vision и Roadmap

1. Откройте файлы:
   - `C:\CursorX\docs\Vision.md`
   - `C:\CursorX\docs\Roadmap.md`

2. Внесите изменения

3. Сохраните файлы

4. Система автоматически обнаружит изменения и перезагрузит контекст

### Принудительная перезагрузка контекста

Если нужно принудительно перезагрузить контекст:

```javascript
// В консоли Node.js
const agent = require('./agents/selfdev');
const a = new agent('./config.json');
a.reloadContext();
```

---

## 📊 Мониторинг процесса

### Логи

Все действия логируются в:
- `/logs/selfdev-YYYY-MM-DD.log`
- Отображаются в UI в боковой панели

### Статус

В заголовке приложения отображается:
- **Готов** - система готова к работе
- **Генерация проекта...** - идет процесс генерации
- **Ошибка** - произошла ошибка

---

## 🔧 Устранение проблем

### Self-Build не работает

1. **Проверьте провайдер:**
   - LM Studio должен быть запущен (если используете локальный)
   - OpenRouter должен быть доступен (если используете API)

2. **Проверьте логи:**
   - Откройте DevTools (F12)
   - Проверьте консоль на ошибки

3. **Проверьте Vision и Roadmap:**
   ```bash
   node -e "const agent = require('./agents/selfdev'); const a = new agent('./config.json'); console.log('Vision:', a.readVision().length); console.log('Roadmap:', a.readRoadmap().length);"
   ```

### AI не отвечает

1. **LM Studio:**
   - Проверьте, что сервер запущен
   - Проверьте, что модели загружены
   - Проверьте порт 1234

2. **OpenRouter:**
   - Проверьте интернет-соединение
   - Проверьте API ключ в `config.json`
   - Проверьте баланс на OpenRouter

---

## 🎓 Рекомендации

1. **Для первого запуска:**
   - Используйте OpenRouter (быстрее и надежнее)
   - Начните с простых задач

2. **Для разработки:**
   - Используйте LM Studio (бесплатно, приватно)
   - Мониторьте логи

3. **Для сложных задач:**
   - Используйте OpenRouter с GPT-4
   - Укажите конкретную задачу в чате

---

## ✅ Готово!

Теперь вы можете запустить Cursor X и начать самопрограммирование:

```bash
npm start
```

Нажмите кнопку **"Self-Build"** и наблюдайте, как система разрабатывает сама себя! 🚀

---

*Документ создан для помощи в запуске самопрограммирования*



