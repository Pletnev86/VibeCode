# Быстрый старт Local Cursor X

## 🚀 Запуск приложения

### Шаг 1: Подготовка LM Studio (опционально, для локальных моделей)

1. Запустите **LM Studio**
2. Загрузите модели:
   - `deepseek/deepseek-r1-0528-qwen3-8b`
   - `nomic-ai-gpt4all-falcon`
3. Убедитесь, что сервер запущен на порту **1234**

**Примечание:** Если не хотите использовать локальные модели, можете сразу переключиться на OpenRouter в UI.

---

### Шаг 2: Запуск приложения

#### Вариант 1: Обычный запуск
```bash
npm start
```

#### Вариант 2: Режим разработки (с DevTools)
```bash
npm run dev
```

---

### Шаг 3: Использование

1. **Откроется окно приложения**

2. **В боковой панели (слева) выберите AI Провайдер:**
   - **LM Studio (локальный)** - для локальных моделей
   - **OpenRouter (API)** - для внешних моделей через API

3. **Используйте функции:**
   - **Кнопка "Self-Build"** - генерация каркаса проекта
   - **Кнопка "Анализ проекта"** - анализ структуры проекта
   - **Чат-интерфейс** - отправка запросов к AI

---

## 📍 Где находится переключатель провайдера?

**В боковой панели слева:**
```
┌─────────────────────┐
│   Self-Build        │
│   Анализ проекта    │
│                     │
│   AI Провайдер      │ ← ЗДЕСЬ
│   ○ LM Studio       │
│   ○ OpenRouter      │
│                     │
│   Логи              │
└─────────────────────┘
```

---

## ⚙️ Настройка провайдеров

### LM Studio (локальный)
- Убедитесь, что LM Studio запущен
- Модели должны быть загружены
- Сервер должен слушать на порту 1234

### OpenRouter (API)
- API ключ уже настроен в `config.json`
- Требуется интернет-соединение
- Автоматически используется при выборе в UI

---

## 🔧 Устранение проблем

### Приложение не запускается

1. Проверьте, что зависимости установлены:
   ```bash
   npm install
   ```

2. Проверьте версию Node.js:
   ```bash
   node --version
   ```
   Должна быть 16+.

### LM Studio недоступен

- Это нормально, если вы используете OpenRouter
- Переключитесь на OpenRouter в UI
- Или запустите LM Studio и загрузите модели

### OpenRouter не работает

- Проверьте интернет-соединение
- Проверьте API ключ в `config.json`
- Проверьте баланс на OpenRouter

---

## 📚 Дополнительная документация

- [USAGE.md](USAGE.md) - Подробная инструкция
- [PROVIDER_SWITCH.md](docs/PROVIDER_SWITCH.md) - Переключение провайдеров
- [ARCHITECTURE.md](docs/ARCHITECTURE.md) - Архитектура системы

---

**Готово! Приложение должно запуститься и открыть окно.**



