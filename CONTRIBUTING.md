# 🤝 Участие в разработке

Спасибо за интерес к проекту VibeCode! Мы приветствуем любой вклад.

## 📋 Содержание

1. [Как начать](#как-начать)
2. [Запуск проекта локально](#запуск-проекта-локально)
3. [Правила веток](#правила-веток)
4. [Текущие задачи](#текущие-задачи)
5. [Процесс разработки](#процесс-разработки)
6. [Стандарты кода](#стандарты-кода)
7. [Создание Pull Request](#создание-pull-request)
8. [Логика работы Issues](#логика-работы-issues)

---

## 🚀 Как начать

### 1. Форкните репозиторий

Нажмите кнопку "Fork" на странице репозитория.

### 2. Клонируйте ваш форк

```bash
git clone https://github.com/ВАШ_USERNAME/VibeCode.git
cd VibeCode
```

### 3. Настройте проект

См. [SETUP.md](SETUP.md) для подробной инструкции.

### 4. Создайте ветку

```bash
git checkout -b feature/название-функции
```

---

## 🚀 Запуск проекта локально

### Быстрый старт

```bash
# 1. Клонируйте репозиторий
git clone https://github.com/Pletnev86/VibeCode.git
cd VibeCode

# 2. Установите зависимости
npm install

# 3. Настройте конфигурацию
cp config.json.example config.json
# Откройте config.json и добавьте API ключи (опционально)

# 4. Запустите приложение
npm start
```

### Режим разработки

```bash
# Запуск с DevTools
npm run dev

# Пересборка native модулей (если нужно)
npm run rebuild
```

### Требования

- **Node.js** 16+ и **npm** 8+
- **LM Studio** (опционально, для локальных моделей)
- **OpenRouter API ключ** (опционально, для облачных моделей)

📖 **Подробная инструкция:** [SETUP.md](SETUP.md)

---

## 🌿 Правила веток

### Соглашение об именовании

Используйте префиксы для типов изменений:

- `feature/` - новая функция
  ```bash
  git checkout -b feature/add-monaco-editor
  ```

- `fix/` - исправление бага
  ```bash
  git checkout -b fix/parsing-files-path
  ```

- `docs/` - документация
  ```bash
  git checkout -b docs/update-readme
  ```

- `refactor/` - рефакторинг
  ```bash
  git checkout -b refactor/improve-router
  ```

- `test/` - тесты
  ```bash
  git checkout -b test/add-unit-tests
  ```

### Структура веток

```
main/master (production)
  ├── develop (development)
  │   ├── feature/xxx
  │   ├── fix/xxx
  │   └── refactor/xxx
```

### Работа с ветками

1. **Всегда создавайте ветку от `master`** (или `develop`, если есть)
2. **Одна ветка = одна задача**
3. **Используйте понятные названия** на английском
4. **Удаляйте ветку после мерджа**

### Синхронизация с upstream

```bash
# Добавьте upstream (один раз)
git remote add upstream https://github.com/Pletnev86/VibeCode.git

# Перед созданием новой ветки синхронизируйтесь
git checkout master
git fetch upstream
git merge upstream/master
```

---

## 🎯 Текущие задачи

### 🔴 Критические (нужны срочно)

#### 1. Исправить базу знаний (better-sqlite3)

**Проблема:** `better-sqlite3` не компилируется на некоторых системах.

**Что нужно:**
- Решить проблему компиляции better-sqlite3
- Или найти альтернативу (node-sqlite3, sql.js)
- Обеспечить graceful degradation (работа без БД)

**Файлы:**
- `lib/knowledge-base.js`
- `src/main.js` (инициализация)

**Приоритет:** 🔴 Высокий

---

#### 2. Улучшить парсинг файлов в Self-Build

**Проблема:** Self-Build иногда создаёт файлы в неправильной директории (`src/src/`).

**Что нужно:**
- Улучшить `parseFilesFromResult()` в `agents/selfdev.js`
- Улучшить нормализацию путей
- Добавить валидацию путей перед сохранением

**Файлы:**
- `agents/selfdev.js` (методы `parseFilesFromResult`, `normalizePath`)

**Приоритет:** 🔴 Высокий

---

#### 3. Восстановить полный UI функционал

**Проблема:** UI файлы были перезаписаны, нужно проверить все функции.

**Что нужно:**
- Проверить все кнопки (Self-Build, Анализ проекта, Доработать модули)
- Проверить переключение провайдеров
- Проверить отображение логов
- Добавить обработку ошибок

**Файлы:**
- `src/index.html`
- `src/ui.js`
- `src/main.js` (IPC обработчики)

**Приоритет:** 🔴 Высокий

---

### 🟡 Важные (нужны в ближайшее время)

#### 4. Реализовать Refactor Agent

**Что нужно:**
- Создать `agents/refactor.js`
- Интегрировать с InterAgentController
- Добавить IPC обработчик в `src/main.js`
- Добавить кнопку в UI

**Приоритет:** 🟡 Средний

**См. пример:** `agents/selfdev.js`

---

#### 5. Реализовать Fix Agent

**Что нужно:**
- Создать `agents/fix.js`
- Анализ ошибок в коде
- Генерация исправлений
- Интеграция с системой

**Приоритет:** 🟡 Средний

---

#### 6. Реализовать Explain Agent

**Что нужно:**
- Создать `agents/explain.js`
- Объяснение кода на русском/английском
- Интеграция с UI

**Приоритет:** 🟡 Средний

---

#### 7. Улучшить обработку ошибок

**Что нужно:**
- Добавить try-catch везде
- Улучшить логирование ошибок
- Показывать понятные сообщения пользователю

**Приоритет:** 🟡 Средний

---

### 🟢 Желательные (можно делать постепенно)

#### 8. Добавить Monaco Editor

**Что нужно:**
- Интегрировать Monaco Editor в UI
- Поддержка подсветки синтаксиса
- Автодополнение кода

**Приоритет:** 🟢 Низкий

---

#### 9. Реализовать автотестирование

**Что нужно:**
- Интеграция с Jest или другим тестовым фреймворком
- Автоматический запуск тестов
- Отображение результатов в UI

**Приоритет:** 🟢 Низкий

---

#### 10. Реализовать PC-Control Agent (AutoIt)

**Что нужно:**
- Интеграция с AutoIt
- Управление ПК через AI
- Безопасное выполнение команд

**Приоритет:** 🟢 Низкий

---

## 🔄 Процесс разработки

### 1. Выберите задачу

- Посмотрите список задач выше
- Или выберите из [docs/Roadmap.md](docs/Roadmap.md)
- Или создайте Issue с описанием проблемы

### 2. Создайте ветку

Следуйте [правилам веток](#правила-веток):

```bash
git checkout -b feature/название-функции
# или
git checkout -b fix/описание-бага
```

### 3. Разработайте

- Пишите код
- Добавляйте комментарии на русском
- Следуйте стандартам кода (см. ниже)

### 4. Протестируйте

```bash
npm start
# Проверьте, что всё работает
```

### 5. Закоммитьте

```bash
git add .
git commit -m "feat: добавлена функция X"
# или
git commit -m "fix: исправлена ошибка Y"
```

**Формат коммитов:**
- `feat:` - новая функция
- `fix:` - исправление бага
- `docs:` - документация
- `refactor:` - рефакторинг
- `test:` - тесты

### 6. Запушьте

```bash
git push origin feature/название-функции
```

### 7. Создайте Pull Request

- Перейдите на GitHub
- Нажмите "New Pull Request"
- Опишите изменения
- Укажите связанные Issues

---

## 📝 Стандарты кода

### Комментарии

```javascript
/**
 * Описание функции
 * 
 * @param {string} param1 - Описание параметра
 * @returns {Promise<Object>} Описание возвращаемого значения
 */
async function myFunction(param1) {
  // Комментарий на русском для сложной логики
  return result;
}
```

### Именование

- **Функции:** `camelCase` - `sendMessage()`
- **Классы:** `PascalCase` - `SelfDevAgent`
- **Константы:** `UPPER_SNAKE_CASE` - `MAX_RETRIES`
- **Файлы:** `kebab-case` - `self-dev-agent.js`

### Структура

```javascript
// 1. Импорты
const fs = require('fs');
const path = require('path');

// 2. Константы
const DEFAULT_TIMEOUT = 30000;

// 3. Класс/функции
class MyClass {
  constructor() {
    // ...
  }
}

// 4. Экспорт
module.exports = MyClass;
```

---

## 🔍 Создание Pull Request

### Шаблон PR

```markdown
## Описание
Краткое описание изменений

## Тип изменения
- [ ] Исправление бага
- [ ] Новая функция
- [ ] Рефакторинг
- [ ] Документация

## Связанные Issues
Closes #123

## Чеклист
- [ ] Код протестирован
- [ ] Документация обновлена
- [ ] Коммиты следуют стандартам
```

### Процесс ревью

1. **Автоматические проверки** - CI/CD (если настроен)
2. **Ревью кода** - проверка другими разработчиками
3. **Тестирование** - проверка работоспособности
4. **Мердж** - после одобрения

---

## 📋 Логика работы Issues

### Типы Issues

#### 🐛 Bug Report (Ошибка)

**Шаблон:**
```markdown
**Описание:**
Краткое описание проблемы

**Шаги для воспроизведения:**
1. Шаг 1
2. Шаг 2
3. ...

**Ожидаемое поведение:**
Что должно было произойти

**Фактическое поведение:**
Что произошло на самом деле

**Окружение:**
- ОС: Windows 10
- Node.js: 18.0.0
- Версия: 0.1.0

**Логи:**
```
Вставьте логи ошибки
```
```

#### ✨ Feature Request (Новая функция)

**Шаблон:**
```markdown
**Описание:**
Что вы хотите добавить

**Проблема:**
Какую проблему это решает

**Решение:**
Как это должно работать

**Альтернативы:**
Другие варианты решения
```

#### 📝 Documentation (Документация)

**Шаблон:**
```markdown
**Что нужно:**
Описание необходимой документации

**Где:**
Где должна быть документация

**Почему:**
Зачем это нужно
```

#### 🔧 Enhancement (Улучшение)

**Шаблон:**
```markdown
**Что улучшить:**
Описание текущего поведения

**Как улучшить:**
Предложение улучшения

**Преимущества:**
Что это даст
```

### Лейблы Issues

- `bug` - Ошибка
- `enhancement` - Улучшение
- `feature` - Новая функция
- `documentation` - Документация
- `help wanted` - Нужна помощь
- `good first issue` - Хорошо для новичков
- `priority: high` - Высокий приоритет
- `priority: medium` - Средний приоритет
- `priority: low` - Низкий приоритет

### Процесс работы с Issues

1. **Создание Issue**
   - Используйте шаблоны выше
   - Добавьте соответствующие лейблы
   - Укажите приоритет

2. **Назначение**
   - Мейнтейнеры назначают исполнителя
   - Или вы можете взять Issue самостоятельно

3. **Работа над Issue**
   - Создайте ветку `fix/issue-123` или `feature/issue-123`
   - Работайте над решением
   - Создайте Pull Request с пометкой `Closes #123`

4. **Закрытие Issue**
   - Issue закрывается автоматически при мердже PR
   - Или вручную, если проблема решена другим способом

### Правила

- ✅ **Один Issue = одна задача**
- ✅ **Используйте понятные названия**
- ✅ **Добавляйте контекст и примеры**
- ✅ **Связывайте Issue с PR через `Closes #123`**
- ❌ **Не создавайте дубликаты** - сначала поищите существующие Issues

---

## 📚 Полезные ресурсы

- [Vision.md](docs/Vision.md) - Видение проекта
- [Roadmap.md](docs/Roadmap.md) - План развития
- [ARCHITECTURE.md](docs/ARCHITECTURE.md) - Архитектура
- [SETUP.md](SETUP.md) - Настройка проекта
- [TROUBLESHOOTING.md](TROUBLESHOOTING.md) - Решение проблем

---

## ❓ Вопросы?

- Создайте Issue на GitHub
- Напишите в Discussions
- Свяжитесь с мейнтейнерами

---

**Спасибо за вклад!** 🎉

