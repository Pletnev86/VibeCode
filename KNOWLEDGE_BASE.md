# База знаний - Документация

## Описание

База знаний на основе SQLite, которая сохраняет запросы, ответы, оценки, методы, решения и скрипты. При работе система сначала ищет ответ в локальной БД, и только потом обращается к ИИ.

## Структура базы данных

### Таблицы

1. **queries** - Запросы пользователей
   - `id` - Уникальный ID
   - `query_text` - Текст запроса
   - `query_hash` - Хеш запроса для быстрого поиска
   - `language` - Язык запроса (ru/en)
   - `task_type` - Тип задачи (code/explanation/reasoning и т.д.)
   - `created_at` - Дата создания

2. **responses** - Ответы от ИИ
   - `id` - Уникальный ID
   - `query_id` - Связь с запросом
   - `response_text` - Текст ответа
   - `model_used` - Использованная модель
   - `provider` - Провайдер (lmstudio/openrouter)
   - `tokens_used` - Использованные токены (для OpenRouter)
   - `time_taken` - Время выполнения (для локальных моделей)
   - `created_at` - Дата создания

3. **ratings** - Оценки ответов
   - `id` - Уникальный ID
   - `response_id` - Связь с ответом
   - `rating` - Оценка (1-5)
   - `is_good` - Флаг хорошего ответа (rating >= 4)
   - `comment` - Комментарий
   - `created_at` - Дата создания

4. **methods** - Методы и решения
   - `id` - Уникальный ID
   - `title` - Название метода
   - `description` - Описание
   - `code` - Код метода
   - `category` - Категория
   - `tags` - Теги (через запятую)
   - `usage_count` - Счетчик использования
   - `success_rate` - Процент успешности
   - `created_at`, `updated_at` - Даты

5. **solutions** - Решения проблем
   - `id` - Уникальный ID
   - `problem` - Описание проблемы
   - `solution` - Решение
   - `method_id` - Связь с методом (опционально)
   - `category` - Категория
   - `tags` - Теги
   - `usage_count` - Счетчик использования
   - `success_rate` - Процент успешности
   - `created_at`, `updated_at` - Даты

6. **scripts** - Скрипты
   - `id` - Уникальный ID
   - `name` - Название скрипта
   - `description` - Описание
   - `code` - Код скрипта
   - `language` - Язык программирования
   - `category` - Категория
   - `tags` - Теги
   - `usage_count` - Счетчик использования
   - `created_at`, `updated_at` - Даты

## Как работает

### 1. Поиск в БД перед обращением к ИИ

При каждом запросе система:
1. Ищет похожие запросы в БД
2. Проверяет, есть ли хорошие ответы (оценка >= 4)
3. Если найден хороший ответ - возвращает его
4. Если не найден - обращается к ИИ

### 2. Сохранение запросов и ответов

После получения ответа от ИИ:
- Запрос сохраняется в таблицу `queries`
- Ответ сохраняется в таблицу `responses`
- Связываются через `query_id`

### 3. Оценка ответов

Пользователь может оценить ответ:
- Кнопки оценки (1-5 звезд) под каждым ответом
- Оценка >= 4 помечается как "хороший ответ"
- Хорошие ответы приоритетны при поиске

### 4. Сохранение методов и решений

Методы и решения можно сохранять вручную:
- Методы - для переиспользования кода
- Решения - для типовых проблем
- Скрипты - для готовых решений

## Использование

### Автоматическое сохранение

Все запросы и ответы автоматически сохраняются в БД.

### Оценка ответов

1. Отправьте запрос
2. Получите ответ
3. Нажмите на кнопку с нужным количеством звезд (1-5)
4. Оценка сохранится в БД

### Поиск в БД

Система автоматически ищет в БД перед обращением к ИИ. Если найден хороший ответ - он будет использован.

## API

### KnowledgeBase класс

```javascript
const KnowledgeBase = require('./lib/knowledge-base');
const kb = new KnowledgeBase('./data/knowledge.db');

// Поиск похожих запросов
const results = kb.searchSimilarQueries('текст запроса', 5);

// Сохранение запроса и ответа
const saved = kb.saveQueryResponse('запрос', 'ответ', {
  model: 'falcon',
  provider: 'lmstudio',
  language: 'ru',
  taskType: 'code'
});

// Сохранение оценки
kb.saveRating(responseId, 5, 'Отличный ответ!');

// Сохранение метода
kb.saveMethod('Название', 'Описание', 'код', 'категория', ['тег1', 'тег2']);

// Сохранение решения
kb.saveSolution('Проблема', 'Решение', methodId, 'категория', ['тег1']);

// Сохранение скрипта
kb.saveScript('Название', 'Описание', 'код', 'javascript', 'категория', ['тег1']);
```

## Расположение БД

База данных сохраняется в: `./data/knowledge.db`

## Индексы

Для быстрого поиска созданы индексы:
- `idx_query_hash` - по хешу запроса
- `idx_query_text` - по тексту запроса
- `idx_response_query` - по связи ответ-запрос
- `idx_rating_response` - по связи оценка-ответ
- `idx_rating_good` - по флагу хорошего ответа

## Производительность

- Используется WAL режим для лучшей производительности
- Индексы для быстрого поиска
- Хеширование запросов для точного совпадения
- Поиск по подстроке для похожих запросов

---

**База знаний готова к использованию!** 🚀




