# 🔧 Инструкция по настройке Local Cursor X

## 📋 Содержание

1. [Быстрая настройка](#быстрая-настройка)
2. [Настройка LM Studio](#настройка-lm-studio)
3. [Настройка OpenRouter](#настройка-openrouter)
4. [Настройка конфигурации](#настройка-конфигурации)
5. [Проверка работы](#проверка-работы)
6. [Решение проблем](#решение-проблем)

---

## 🚀 Быстрая настройка

### Шаг 1: Клонирование и установка

```bash
# Клонируйте репозиторий
git clone https://github.com/Pletnev86/VibeCode.git
cd VibeCode

# Установите зависимости
npm install
```

### Шаг 2: Создание конфигурации

```bash
# Скопируйте пример конфигурации
cp config.json.example config.json
```

### Шаг 3: Настройка API ключей

Откройте `config.json` и добавьте ваши API ключи (см. разделы ниже).

---

## 🤖 Настройка LM Studio

### Что такое LM Studio?

**LM Studio** - это приложение для запуска локальных языковых моделей на вашем компьютере. Это позволяет использовать AI без интернета и без оплаты API.

### Установка LM Studio

1. Скачайте LM Studio с официального сайта: https://lmstudio.ai/
2. Установите приложение
3. Запустите LM Studio

### Загрузка моделей

1. В LM Studio перейдите на вкладку **"Search"**
2. Найдите и загрузите следующие модели:
   - **`deepseek/deepseek-r1-0528-qwen3-8b`** - для reasoning, объяснений, переводов
   - **`nomic-ai-gpt4all-falcon`** - для генерации кода (только английский)

### Запуск сервера

1. В LM Studio перейдите на вкладку **"Local Server"**
2. Выберите одну из загруженных моделей
3. Нажмите **"Start Server"**
4. Убедитесь, что сервер запущен на порту **1234**
5. Статус должен быть **"Server is running"**

### Настройка в config.json

В файле `config.json` настройки LM Studio уже настроены:

```json
"lmStudio": {
  "enabled": true,
  "baseUrl": "http://localhost:1234/v1",
  "models": {
    "deepseek": "deepseek/deepseek-r1-0528-qwen3-8b",
    "falcon": "nomic-ai-gpt4all-falcon"
  },
  "timeout": 120000
}
```

**Важно:** Убедитесь, что:
- ✅ LM Studio запущен
- ✅ Сервер запущен на порту 1234
- ✅ Модель загружена и активна

---

## 🌐 Настройка OpenRouter

### Что такое OpenRouter?

**OpenRouter** - это сервис, который предоставляет доступ к различным AI моделям (GPT-4, Claude, DeepSeek и др.) через единый API.

### Получение API ключа

1. Перейдите на сайт: https://openrouter.ai/
2. Зарегистрируйтесь или войдите в аккаунт
3. Перейдите в раздел **"Keys"** (https://openrouter.ai/keys)
4. Нажмите **"Create Key"**
5. Скопируйте созданный ключ (начинается с `sk-or-v1-...`)

### Настройка в config.json

Откройте `config.json` и найдите секцию `openRouter`:

```json
"openRouter": {
  "enabled": true,
  "apiKey": "ВАШ_OPENROUTER_API_KEY_ЗДЕСЬ",  // ← Вставьте ваш ключ сюда
  "baseUrl": "https://openrouter.ai/api/v1",
  "timeout": 60000,
  "defaultModel": "openai/gpt-4o-mini",
  "selectedModel": "gpt4",
  "models": {
    "gpt4": "openai/gpt-4o-mini",
    "claude": "anthropic/claude-3.5-sonnet",
    "deepseek": "deepseek/deepseek-chat"
  },
  "autoFallback": true,
  "checkTokens": true
}
```

**Замените** `"ВАШ_OPENROUTER_API_KEY_ЗДЕСЬ"` на ваш реальный ключ.

### Доступные модели

Вы можете выбрать любую из доступных моделей:

- **`openai/gpt-4o-mini`** - быстрая и дешевая модель от OpenAI
- **`anthropic/claude-3.5-sonnet`** - мощная модель от Anthropic
- **`deepseek/deepseek-chat`** - недорогая альтернатива

Измените `"selectedModel"` на нужную модель.

### Проверка баланса токенов

OpenRouter показывает баланс токенов на странице аккаунта. Если токены закончатся, система автоматически переключится на LM Studio (если `autoFallback: true`).

---

## ⚙️ Настройка конфигурации

### Структура config.json

```json
{
  "ai": {
    "providers": {
      "lmStudio": { ... },      // Локальные модели
      "openRouter": { ... },    // Внешние модели через API
      "gpt": { ... }            // Прямой доступ к OpenAI (опционально)
    },
    "smartAutoMode": { ... }    // Автоматический выбор модели
  },
  "agents": {
    "selfdev": { ... }          // SelfDev Agent настройки
  },
  "execution": { ... }          // Настройки выполнения
}
```

### Включение/отключение провайдеров

Чтобы использовать только LM Studio:
```json
"lmStudio": {
  "enabled": true
},
"openRouter": {
  "enabled": false
}
```

Чтобы использовать только OpenRouter:
```json
"lmStudio": {
  "enabled": false
},
"openRouter": {
  "enabled": true,
  "apiKey": "ваш_ключ"
}
```

### Smart Auto Mode

Автоматически выбирает модель по типу задачи:

```json
"smartAutoMode": {
  "enabled": true,
  "defaultModel": "falcon",           // Модель по умолчанию
  "fallbackModel": "deepseek",        // Резервная модель
  "openRouterFallback": true,         // Использовать OpenRouter как fallback
  "checkTokens": true                 // Проверять баланс токенов
}
```

---

## ✅ Проверка работы

### Запуск приложения

```bash
npm start
```

### Проверка LM Studio

1. Убедитесь, что LM Studio запущен
2. В приложении выберите провайдер **"LM Studio"**
3. Отправьте тестовое сообщение
4. Должен прийти ответ от локальной модели

### Проверка OpenRouter

1. Убедитесь, что API ключ добавлен в `config.json`
2. В приложении выберите провайдер **"OpenRouter"**
3. Отправьте тестовое сообщение
4. Должен прийти ответ от выбранной модели

### Проверка Self-Build

1. Нажмите кнопку **"Self-Build"** в интерфейсе
2. Система должна прочитать Vision.md и Roadmap.md
3. Должны сгенерироваться файлы проекта

---

## 🔧 Решение проблем

### Проблема: LM Studio не отвечает

**Решение:**
1. Проверьте, что LM Studio запущен
2. Проверьте, что сервер запущен на порту 1234
3. Проверьте, что модель загружена и активна
4. Проверьте настройки в `config.json`: `baseUrl: "http://localhost:1234/v1"`

### Проблема: OpenRouter возвращает ошибку

**Решение:**
1. Проверьте, что API ключ правильно скопирован в `config.json`
2. Проверьте баланс токенов на https://openrouter.ai/
3. Проверьте интернет-соединение
4. Попробуйте другую модель

### Проблема: База знаний не работает

**Решение:**
1. Убедитесь, что установлен `better-sqlite3`:
   ```bash
   npm install better-sqlite3
   ```
2. Если ошибка компиляции, попробуйте:
   ```bash
   npm run rebuild
   ```
3. Система продолжит работать без базы знаний, но без сохранения истории

### Проблема: Файлы создаются в неправильной директории

**Решение:**
1. Проверьте, что `src/main.js` использует правильные пути
2. Убедитесь, что файлы сохраняются в `src/`, а не `src/src/`

---

## 📚 Дополнительная документация

- [README.md](README.md) - Общее описание проекта
- [QUICK_START.md](QUICK_START.md) - Быстрый старт
- [USAGE.md](USAGE.md) - Инструкция по использованию
- [TROUBLESHOOTING.md](TROUBLESHOOTING.md) - Решение проблем
- [docs/PROVIDER_SWITCH.md](docs/PROVIDER_SWITCH.md) - Переключение провайдеров

---

## 🆘 Нужна помощь?

Если у вас возникли проблемы:

1. Проверьте [TROUBLESHOOTING.md](TROUBLESHOOTING.md)
2. Создайте Issue на GitHub: https://github.com/Pletnev86/VibeCode/issues
3. Проверьте логи в консоли приложения (DevTools: F12)

---

**Готово!** Теперь вы можете использовать Local Cursor X. 🎉

